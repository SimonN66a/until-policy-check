{
  "name": "until-policy-check",
  "version": "1.3.0",
  "private": true,
  "type": "module",
  "description": "UNTIL health insurance policy checker. Reads a member's policy and reports which wellness services are covered, quoting the wording behind every verdict.",
  "dependencies": {
    "@anthropic-ai/sdk": "^0.71.0",
    "@supabase/supabase-js": "^2.115.0"
  },
  "devDependencies": {
    "pg": "^8.13.1"
  },
  "scripts": {
    "build": "node scripts/build-css.mjs",
    "dev": "node scripts/build-css.mjs && node scripts/dev-server.mjs",
    "test": "node scripts/test-api.mjs",
    "test:shim": "node scripts/_test-shim.mjs"
  }
}